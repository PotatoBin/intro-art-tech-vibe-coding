Please deploy this project using the Vercel CLI.

Use the current project files and workspace context.
Do not change the project concept, design direction, or major implementation just for deployment.
Only make deployment-related fixes if they are necessary.

Before deploying, check the following:

1. Project Status
- Identify the project type, framework, package manager, and available run/build scripts.
- Run install, lint, test, or build commands if available.
- If a command fails, explain the failure and fix only what is necessary for deployment.

2. Submission Readiness
- Check that the submission page includes the required content for the selected assignment option.
- For Option 1, check that the project landing page is included.
- For Option 1, if the output cannot be directly experienced on the web, check that a demo video, screenshots, or explanation is included.
- For Option 2, check that the work can be directly accessed and experienced on the web.
- Check that API keys, tokens, passwords, personal information, `.env`, and local config files are not exposed.
- If external assets are used, check for permission, attribution, and license issues.

3. Vercel CLI Deployment
- Check whether the Vercel CLI is available.
- If it is not installed, install it using the appropriate package manager.
- Deploy to production using the Vercel CLI.
- If login, project linking, team selection, or environment variables are required, clearly explain what input is needed.

4. After Deployment
- Provide the deployed Vercel URL.
- Open the deployed URL in a private/incognito browser and verify that it loads.
- Test that the main interaction and key assets work in the deployed environment.
- Check for major console errors and mobile usability issues.
- Summarize what was checked, any deployment-related changes, and any remaining risks.

Important constraints:
- Do not change the core concept or tech stack without a clear blocker.
- Do not add major new features.
- Do not hide build errors, sensitive information exposure, or external asset rights issues.
- Do not claim deployment succeeded unless a Vercel URL is available.
